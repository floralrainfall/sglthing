#include "map.h"


static bool can_generate(int center, int north_path, int south_path, int west_path, int east_path)
{
    switch(center)
    {

    }
}

void map_generate(struct map* map)
{
    for(int x = 0; x < MAP_MAX_TILES_X; x++)
        for(int y = 0; y < MAP_MAX_TILES_Y; y++)
        {

        }
}